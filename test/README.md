# Tests

Run the isolated save, apply, backup, pruning, and dry-run integration test:

```sh
test/run
```

The test uses a temporary repository, `HOME`, and `XDG_STATE_HOME`; it does not modify the live installation.
