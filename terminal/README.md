# Terminal.app

`Default.terminal` is the Terminal.app profile, including the Berkeley Mono font.

To install it, open `Default.terminal` in Finder (or run `open Default.terminal`), then choose it as Terminal's default profile in **Terminal → Settings → Profiles**.
